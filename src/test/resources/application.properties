# ===============================
# Database configuration for tests (H2 in memory)
# ===============================
spring.datasource.url=jdbc:h2:mem:peliculas_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate configuration for tests
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true

# RabbitMQ dummy settings for context loading
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
app.rabbitmq.exchange=pelicula_exchange
app.rabbitmq.routing.key=pelicula.event

<<<<<<< HEAD
# Defer datasource initialization so schema is created before data.sql runs
spring.jpa.defer-datasource-initialization=true

=======
# Historial
historial.base-url=http://localhost:8083
>>>>>>> eb9036f (Integración de Keycloak y mejoras en películas)
